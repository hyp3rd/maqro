/** Re-export shim — goal-phase logic moved to `@maqro/core`. Kept so the
 *  existing `@/lib/goal-phases` imports keep working unchanged. Import from
 *  `@maqro/core` directly in new code. */
export * from "@maqro/core/goal-phases";
