/** Re-export shim — weekly-recap logic moved to `@maqro/core`. Kept so the
 *  existing `@/lib/weekly-recap` imports keep working unchanged. Import from
 *  `@maqro/core` directly in new code. */
export * from "@maqro/core/weekly-recap";
