/** Re-export shim — moved to `@maqro/core/streaks`. Kept so existing `@/lib/streaks`
 *  imports keep working unchanged; import from `@maqro/core/streaks` in new code. */
export * from "@maqro/core/streaks";
