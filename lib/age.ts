/** Re-export shim — age derivation moved to `@maqro/core`. Kept so the existing
 *  `@/lib/age` imports keep working unchanged. Import from `@maqro/core`
 *  directly in new code. */
export * from "@maqro/core/age";
