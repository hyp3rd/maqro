/** Re-export shim — supplement logic lives in `@maqro/core/supplements`. Kept so
 *  `@/lib/supplements` resolves like the other domain modules; import from
 *  `@maqro/core/supplements` in new code. */
export * from "@maqro/core/supplements";
