/** Re-export shim — moved to `@maqro/core/body-fat`. Kept so existing `@/lib/body-fat`
 *  imports keep working unchanged; import from `@maqro/core/body-fat` in new code. */
export * from "@maqro/core/body-fat";
