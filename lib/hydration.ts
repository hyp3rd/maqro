/** Re-export shim — moved to `@maqro/core/hydration`. Kept so existing `@/lib/hydration`
 *  imports keep working unchanged; import from `@maqro/core/hydration` in new code. */
export * from "@maqro/core/hydration";
