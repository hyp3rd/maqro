/** Re-export shim — trend / adaptive-TDEE logic moved to `@maqro/core`. Kept so
 *  the existing `@/lib/trends` imports keep working unchanged. Import from
 *  `@maqro/core` directly in new code. */
export * from "@maqro/core/trends";
