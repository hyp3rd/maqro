/** Pure intermittent-fasting math now lives in `@maqro/core/fasting` (shared
 *  with the native app). Re-exported here so `@/lib/fasting` consumers are
 *  unchanged. */
export * from "@maqro/core/fasting";
